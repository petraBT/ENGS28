<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns:xi="http://www.w3.org/2001/XInclude" xml:id="ch-accelerometers">
    <title>Accelerometers</title>
    <introduction>
        <p>
            The goal of this chapter is to measure acceleration with the
            LSM303AGR, a three-axis accelerometer, and to use what it measures
            to compute how far the sensor is tilted — the same computation your phone
            does when it decides to rotate its screen.  The sensor is an I2C
            (Inter-Integrated Circuit) device, so everything we built in
            <xref ref="ch-i2c"/> carries
            over: we will wire it to the STM32C031C6's I2C bus, verify the
            link, look at what is inside the sensor, and complete a device
            driver for it.
            <xref ref="sec-accel-reference"/> collects the register map, the
            data-format arithmetic, and the floating-point details for looking
            up later.
        </p>
        <objectives>
            <ul>
                <li>Wire a second device onto the I2C bus and verify the link by reading its WHO_AM_I register.</li>
                <li>Debug an I2C bus with the Analog Discovery 2's logic analyzer and protocol decoder.</li>
                <li>Explain how a MEMS (Micro-Electro-Mechanical Systems) accelerometer converts acceleration into a digital reading.</li>
                <li>Convert the sensor's raw output — 16-bit, left-justified, two's complement — into milli-g.</li>
                <li>Complete, test, and package a device driver for the LSM303AGR.</li>
                <li>Compute tilt angles from gravity's projection, using floating-point arithmetic on a processor without floating-point hardware.</li>
            </ul>
        </objectives>
    </introduction>

    <!-- ============================================================
         Day 13 Before Class.  Week 7 session 2 (plans/day13-prompt.md)
         writes this section: I2C recalled in one page leaning on ch-i2c
         xrefs; the LSM303AGR introduced with the breakout-board and
         STEMMA definitions from plans/day13.md Hand-offs; WHO_AM_I and
         why self-identifying registers exist; reading questions (B-3).
         MUST NOT contain: MEMS internals (Day 13x), the 0x19 derivation
         (Part 5), any trace walkthrough (Part 4), the wrong-address
         outcome (Q3 open).
         ============================================================ -->
    <section xml:id="sec-accel-before-class">
        <title>Before Class: A Second I2C Device</title>
        <introduction>
            <p>
                On Day 13 we'll add a new device to our I2C bus: an
                accelerometer.  This reading recalls how the bus works and
                introduces the sensor; in class we will wire it up, prove that
                we can talk to it, and look at the signals on the two wires
                with the logic analyzer.
            </p>
        </introduction>

        <subsection xml:id="subsec-accel-i2c-recall">
            <title>The Bus You Already Have</title>
            <p>
                The I2C bus we built in
                <xref ref="ch-i2c"/> has two wires: SDA (serial data) and SCL
                (serial clock), both held HIGH by pull-up resistors whenever no
                device is transmitting.  One device is in charge of the bus —
                the <term>controller</term>, which for us is the STM32C031C6 —
                and every other device on the bus is a <term>target</term>.
                Each target has its own 7-bit address, and every transaction
                begins with the controller sending the address of the device it
                wants to talk to.  All the targets hear that address; the one
                that recognizes it as its own acknowledges and takes part in
                the rest of the transaction, and the other devices on the bus
                don't respond.  If you'd like to review more than this
                paragraph covers, go to <xref ref="sec-i2c-reference"/>, which
                has the protocol in full — START and STOP, the ACK handover,
                and the byte-by-byte framing.
            </p>
            <p>
                The STM32C031C6's I2C peripheral produces all of that bit-level
                traffic in hardware, and the I2C library we handed you is what
                tells it what to send.  We have been calling that library ever
                since we first wired up the display, and its five functions —
                <c>i2c1_init()</c>, <c>i2c1_byteWrite()</c>,
                <c>i2c1_byteRead()</c>, <c>i2c1_memWrite()</c>, and
                <c>i2c1_memRead()</c> — are all we need for the accelerometer,
                too.  The seven-segment display driver you wrote is built on
                top of them.  The code for all five functions, with the
                register-level detail of how each one works, is in
                <xref ref="subsec-i2c-ref-library"/>.
            </p>
        </subsection>

        <subsection xml:id="subsec-accel-meet-lsm303agr">
            <title>The LSM303AGR and Its Breakout Board</title>
            <p>
                The new device is the LSM303AGR, a sensor chip made by
                STMicroelectronics that measures acceleration along three
                perpendicular axes.  The chip also contains a magnetometer, but
                we'll only use the accelerometer in this class.  We interact
                with it via I2C; the other option would be SPI (Serial
                Peripheral Interface), another serial communication protocol,
                but the board we use is wired for I2C by default.
            </p>
            <p>
                The chip itself is a couple of millimeters across, far too
                small to handle directly.  It comes to us on a
                <term>breakout board</term> — a small board that carries the
                chip and the extra parts it needs, in this case the bus's
                pull-up resistors and a voltage regulator.  The breakout
                connects by a STEMMA QT cable, a 4-wire plug that only goes in
                one way, so it cannot be miswired.  We'll do the wiring
                together in class.
            </p>
        </subsection>

        <subsection xml:id="subsec-accel-whoami">
            <title>The WHO_AM_I Register</title>
            <p>
                Like the HT16K33 on the display's backpack
                (<xref ref="ch-i2c"/>), the accelerometer is operated entirely
                through its internal registers: each register has an address inside the device, we
                configure the sensor by writing to some of them, and we get
                measurements out by reading others.  Which registers exist, and
                what each bit in them means, comes from the device's datasheet
                — for this chapter, the
                <url href="external/datasheets/lsm303agr.pdf">LSM303AGR
                datasheet</url>.
            </p>
            <p>
                One of those registers exists purely so that you can check the
                connection: the special register called WHO_AM_I always
                contains the value 0x33.  It is read-only, and its contents are
                fixed when the chip is manufactured
                (Datasheet §8.4, register WHO_AM_I_A, address 0x0F — the
                default value 00110011 is 0x33).  If you read the WHO_AM_I
                register and get the right result, you can be confident in
                your I2C setup: the wiring, the device address, and your whole
                read path all had to work for that 0x33 to arrive.
            </p>
            <p>
                Nearly every I2C sensor has a register like this, because a
                value that is known in advance gives you something to check
                against before you rely on any of the sensor's measurements.  Device drivers
                use it the same way: a driver's initialization function
                typically reads WHO_AM_I first, and reports failure if the
                expected value does not come back.  In class we'll run a short
                test program that does exactly this check over your own wiring.
            </p>
        </subsection>

        <reading-questions xml:id="rq-accel-day13">
            <exercise xml:id="rq-accel-addressing">
                <statement><p>The I2C bus has only two wires, shared by every
                device on it.  When the controller starts a transaction, how
                does the right device know the transaction is meant for
                it?</p></statement>
                <choices randomize="yes">
                    <choice correct="yes">
                        <statement><p>Every transaction begins with a 7-bit
                        device address.  All the targets hear it, and only the
                        device that recognizes the address as its own
                        acknowledges and takes part.</p></statement>
                        <feedback><p>Correct.  Addressing is what lets many
                        devices share the same two wires — each target answers
                        only to its own address.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p>Each device is wired to the controller
                        through its own pair of SDA and SCL lines.</p></statement>
                        <feedback><p>Separate wires per device is exactly what
                        I2C avoids — that was UART's limitation.  All I2C
                        devices share one SDA and one SCL, and the address at
                        the start of each transaction says who is being
                        spoken to.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p>The devices take turns in a fixed order,
                        one transaction each.</p></statement>
                        <feedback><p>There is no rotation schedule on the bus.
                        The controller decides whom to address, whenever it
                        wants, by sending that device's address first.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p>The controller raises SCL to a higher
                        voltage for the device it wants.</p></statement>
                        <feedback><p>SCL is the shared clock, and it only ever
                        switches between LOW and 3.3 V.  Selection happens with
                        data — the 7-bit address sent at the start of every
                        transaction — not with voltage levels.</p></feedback>
                    </choice>
                </choices>
            </exercise>

            <exercise xml:id="rq-accel-memread">
                <statement><p>To read the accelerometer's WHO_AM_I register,
                the program must tell the sensor <em>which</em> register it
                wants and then collect the byte stored there.  Which I2C
                library function does that whole job?</p></statement>
                <choices randomize="yes">
                    <choice correct="yes">
                        <statement><p><c>i2c1_memRead()</c> — it takes the
                        device address <em>and</em> a register address, and
                        reads one or more bytes from that location inside the
                        target.</p></statement>
                        <feedback><p>Correct.  Reading a specific register is a
                        two-step conversation — say which register, then read —
                        and <c>i2c1_memRead()</c> carries out both
                        steps.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p><c>i2c1_byteRead()</c> — it reads one
                        byte from a target device.</p></statement>
                        <feedback><p><c>i2c1_byteRead()</c> takes only a device
                        address — there is no way to tell the sensor which of
                        its many registers you mean.  Reading a specific
                        register needs <c>i2c1_memRead()</c>, which takes the
                        register address too.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p><c>i2c1_memWrite()</c> — it sends the
                        register address to the device.</p></statement>
                        <feedback><p><c>i2c1_memWrite()</c> does send a
                        register address, but what follows is data written
                        <em>into</em> the device.  To get the register's
                        contents back out, the function is
                        <c>i2c1_memRead()</c>.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p><c>i2c1_init()</c> — it sets up the bus,
                        which includes scanning the registers.</p></statement>
                        <feedback><p><c>i2c1_init()</c> configures the
                        STM32C031C6's own I2C peripheral and pins.  It sends
                        nothing to any target device, and it does not read
                        registers.</p></feedback>
                    </choice>
                </choices>
            </exercise>

            <exercise xml:id="rq-accel-whoami-purpose">
                <statement><p>Your program reads the accelerometer's WHO_AM_I
                register and gets 0x33, the expected value.  What does that
                tell you?</p></statement>
                <choices randomize="yes">
                    <choice correct="yes">
                        <statement><p>The I2C link works end to end: the
                        wiring, the device address, and the read path all
                        functioned, because the expected value arrived over
                        them.</p></statement>
                        <feedback><p>Correct.  WHO_AM_I's value is known in
                        advance, so getting it back is evidence about the
                        <em>communication</em>, which is what the register is
                        for.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p>The accelerometer is configured and its
                        measurements are ready to use.</p></statement>
                        <feedback><p>WHO_AM_I says nothing about
                        configuration — its value is fixed at manufacture and
                        never changes.  Setting the sensor's mode and range is
                        separate work, done through other registers.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p>The sensor's I2C address is
                        0x33.</p></statement>
                        <feedback><p>0x33 is the <em>contents</em> of the
                        WHO_AM_I register, not the device's bus address.  To
                        read the register at all, the program already had to
                        know the device's address and use it.</p></feedback>
                    </choice>
                    <choice>
                        <statement><p>The sensor passed its power-on self-test
                        of the measurement hardware.</p></statement>
                        <feedback><p>Reading WHO_AM_I exercises none of the
                        measurement hardware — the register is a fixed
                        constant.  What the successful read demonstrates is
                        that the bus and your read path work.</p></feedback>
                    </choice>
                </choices>
            </exercise>
        </reading-questions>
    </section>

    <!-- ============================================================
         Day 13 In-Class.  Week 7 session 2 (plans/day13-prompt.md)
         writes the Part 1..8 skeleton per plans/day13.md: activities,
         <instructor> answers, figures, <slide> blocks — connecting
         prose follows Petra's pass.
         ============================================================ -->
    <section xml:id="sec-accel-day13">
        <title>Day 13 In-Class: The Accelerometer as an I2C Device</title>
        <p>
            This section is still being written.
        </p>
    </section>

    <!-- ============================================================
         Day 13x In-Class.  Week 7 session 3 (plans/day13x-prompt.md)
         fills this section per plans/day13x.md: the proof-mass physics,
         MEMS, the datasheet characteristics table, the data format
         (two's complement is FIRST TEACHING here), auto-increment and
         byte assembly, the control registers.  x-day: no reading.

         PARKED below (commented out): the salvageable pieces of the
         rough chapter, none of them trusted (ground truth §7).  Figures
         are rebuilt per the manifest (ground truth §6), not reused as
         extracted.
         ============================================================ -->
    <section xml:id="sec-accel-day13x">
        <title>Day 13x In-Class: What a MEMS Accelerometer Is</title>
        <p>
            This section is still being written.
        </p>

        <!-- PARKED from the rough chapter, for session 3 to mine — do not
             ship any of it unchecked:

        (a) Proof-mass prose (rough subsec-proof-mass).  The kx = ma
            derivation and the capacitive-MEMS description broadly match
            her slides 3–5, but session 3 rewrites from the deck (P-12);
            the piezoelectric/piezoresistive aside is NOT in her deck.

        <subsection xml:id="subsec-proof-mass">
            <title>The Proof Mass: Turning Acceleration into a Signal</title>
            <p>
                Inside every accelerometer is a small <term>proof mass</term> — a tiny
                mass suspended by springs from the sensor's housing.  When the housing
                accelerates, the proof mass tends to stay behind (inertia), so it
                displaces relative to the housing.  The spring pulls it back, and at
                equilibrium the spring force equals the inertial force ... x = ma/k.
                Measuring the displacement of the proof mass gives you the acceleration.
            </p>
            <figure xml:id="fig-accel-proof-mass">
                REBUILD per manifest 13x/3 (mass-spring-case diagram with labels);
                the raw pull is images/Day13x-Accelerometer/slide03_4c6909d1.png.
            </figure>
            <p>
                In a capacitive MEMS (Micro-Electro-Mechanical Systems) accelerometer,
                the proof mass and springs are etched from a thin silicon wafer ...
                The proof mass forms one plate of a capacitor, and fixed plates on
                either side form the other plates.  As the mass displaces, the
                capacitance to one fixed plate increases and to the other decreases.
                [C1/C2 differential detail goes to Reference — Gate 1.]
            </p>
            <figure xml:id="fig-accel-mems">
                REBUILD per manifest 13x/5 (MEMS micrograph + mechanism; watch the
                multi-picture composite trap); raw pull: slide05_a4db99e5.png.
            </figure>
        </subsection>

        (b) Block diagram figure.  REBUILD per manifest 13x/7 with the
            "we only use the accelerometer" callout; raw pull:
            images/Day13x-Accelerometer/slide07_70371632.png.  The rough
            chapter (fig-lsm303agr-chip) captioned this image as a module
            photo — it is the block diagram; do not reuse that caption.

        (c) Characteristics-table figure (rough fig-lsm303agr-specs).
            REBUILD per manifest 13x/8 — commit-time image must MASK the
            sensitivity column (plans/day13x.md Part 3).  Raw pulls:
            slide08_e77cd560.png, slide08_15a6b647.png.  The rough caption
            said "±2g full-scale and 12-bit low-power mode" — WRONG twice
            (ground truth §7 items 1–2): low power is 8-bit, and her
            settings (CTRL_REG4 = 0x00) select normal 10-bit mode.

        (d) Breakout schematic figure (rough fig-lsm303agr-breakout).
            REBUILD per manifest 13x/9 (callouts: pull-ups, header,
            regulator, I2C strapping); raw pull: slide09_7c9b0a0a.png.

        (e) Data-format prose (rough subsec-accel-data-format): output is
            16-bit, left-justified two's complement; a = 4·raw/2^16 at
            ±2 g.  The rough version's "In 12-bit low-power mode, only
            bits [15:4] are valid" repeats conviction (2) above — the
            course's mode is normal 10-bit.  Session 3 teaches this per
            plans/day13x.md Part 4 (staged: unsigned byte → sign →
            collapse → committed negative conversion).

        (f) ST transfer-diagram figure (rough fig-accel-i2c-transfers,
            image slide17_c59c860f.png = 13x/17): same base as Day 13's
            transfer-patterns figure; 13x Part 4 beat 5 quotes §6.1.1
            auto-increment against it.
        -->
    </section>

    <!-- ============================================================
         Day 14 Before Class.  Week 7 session 4 (plans/day14-prompt.md)
         writes this section: the datasheet scavenger hunt (CTRL_REG1/4
         bit fields; the ANSWERS 0x77/0x00 stay out of the reading), plus
         the stationary-reading subsection in her decks' framing (Q8:
         flat ≈ +1000 mg, flipped ≈ −1000 mg; NO proper-acceleration/
         free-fall hook).  Citations: §8.6 Tables 33–35 (p. 47), §8.9
         Tables 41–42 (p. 49) of external/datasheets/lsm303agr.pdf.
         ============================================================ -->
    <section xml:id="sec-accel-day14-before">
        <title>Before Class: The Datasheet Scavenger Hunt</title>
        <p>
            This section is still being written.
        </p>
    </section>

    <!-- ============================================================
         Day 14 In-Class.  Week 7 session 4 (plans/day14-prompt.md)
         fills this section per plans/day14.md: settings reveal, driver
         completion, accel_test.c, library promotion, CoolTerm chart,
         tilt, floating point, Lab 7.

         PARKED below (commented out) from the rough chapter:

        (a) Tilt-geometry figure (rough fig-tilt-geometry).  REBUILD per
            manifest 14/17 with the formula callouts; raw pulls:
            images/Day14-Accelerometer(2)/slide17_d3f656d6.png +
            slide17_fe7eb394.png.  The rough caption's arcsine framing is
            the route AN-1057 shows FAILS near 90° — the class lands
            atan2 (ground truth §7 item 4); keep the sin θ mechanism only
            as the single-axis contrast.

        (b) CoolTerm plotter figure (rough fig-accel-plotter).  KEEP the
            image per manifest 14/15 (slide15_b19d5940.png).  The rough
            caption's menu path "Connection → Serial Plotter" is wrong —
            her slide says View → View Chart; session 4 confirms the
            wording against the current CoolTerm (Q7) before captioning.

         DELETED, not parked (ground truth §7): the sinf()/asinf()
         paragraph (item 4), the "tilt display updating at 100 Hz ...
         easily fast enough" invention (item 5), and the rq-accel-gravity
         reading question (normal-force framing — Q8).
         ============================================================ -->
    <section xml:id="sec-accel-day14">
        <title>Day 14 In-Class: Finish the Driver, See the Data, Measure Tilt</title>
        <p>
            This section is still being written.
        </p>
    </section>

    <!-- ============================================================
         Reference.  Week 7 session 4 (plans/day14-prompt.md) decides and
         writes it; sibling shape is sec-i2c-reference ("nothing here is
         new" opener, then lookup-form subsections).  Manifest (from
         plans/week7.md and Gate 1 items 5, 18):
           - the LSM303AGR register map / lsm303agr.h
           - the data-format arithmetic in full, with the worked negative
             example
           - the signed-shift note (>> is an arithmetic floor shift)
           - IEEE 754 layout; precision vs range said precisely
           - AN-1057 depth (three-angle orientation, quadrants) —
             external/datasheets/an-1057.pdf is hosted (2026-08-27)
           - zero-g offset calibration (flat + flipped, average)
           - the C1/C2 differential-capacitance detail from Day 13x
         ============================================================ -->
    <section xml:id="sec-accel-reference">
        <title>Reference: The LSM303AGR and Floating Point</title>
        <p>
            This section is still being written.
        </p>
    </section>

</chapter>
